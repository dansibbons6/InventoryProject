Inventory project provides a simple inventory system that keeps a record of inventory items in a simple manner.
Basic features of inventory system include, adding an item, deleting an item, searching item, exiting the system.
Inventory system has a basic logon and password system.
Run the inventory system from command line or in the PyCharm IDE.
